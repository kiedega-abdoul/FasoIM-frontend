import { useEffect, useState } from "react"
import { useNavigate, useParams } from "react-router-dom"
import { Plus, Trash2 } from "lucide-react"
import { getApiErrorMessage } from "@/api/api-error"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { ErrorBox, Loading, PageHeader } from "@/features/accounts/components"
import { sessionsApi } from "../api"
import { MODE_LABELS } from "../labels"
import type { ImmersionSession, SessionParametersPayload } from "../types"
const selectClass = "h-12 w-full rounded-xl border bg-background px-3 text-base"
const textAreaClass = "w-full rounded-xl border bg-background px-4 py-3 text-base outline-none focus:border-primary focus:ring-2 focus:ring-primary/20"
const empty: SessionParametersPayload = { mode_entree: "import", hebergement_active: true, repas_active: true, visite_medicale_active: false, mode_visite_medicale: "arrivee", activites_active: true, evaluation_active: false, attestation_active: true, consultation_publique_active: false, taux_presence_minimum_attestation: 80, moyenne_minimum_attestation: 10, directives_generales: "", consignes_generales: "", documents_exiges: [] }
export function SessionParametersPage({ configure = false }: { configure?: boolean }) {
 const { sessionId } = useParams(); const navigate = useNavigate(); const [session,setSession]=useState<ImmersionSession|null>(null); const [form,setForm]=useState<SessionParametersPayload>(empty); const [document,setDocument]=useState(""); const [loading,setLoading]=useState(true); const [submitting,setSubmitting]=useState(false); const [error,setError]=useState("")
 useEffect(()=>{ if(!sessionId)return; sessionsApi.session(Number(sessionId)).then((s)=>{setSession(s); if(s.parametres) setForm(s.parametres)}).catch((e)=>setError(getApiErrorMessage(e))).finally(()=>setLoading(false))},[sessionId])
 function setField<K extends keyof SessionParametersPayload>(k:K,v:SessionParametersPayload[K]){setForm(c=>({...c,[k]:v}))}
 async function submit(e:React.FormEvent){e.preventDefault(); if(!sessionId||!session)return; setSubmitting(true); setError(""); try{ if(configure) await sessionsApi.configureParameters(Number(sessionId),form); else if(session.parametres) await sessionsApi.updateParameters(session.parametres.id,form); navigate(`/app/sessions/${sessionId}`)}catch(ex){setError(getApiErrorMessage(ex))}finally{setSubmitting(false)}}
 if(loading)return <Loading/>; if(!session)return <ErrorBox message={error||"Session introuvable."}/>; if(configure&&session.parametres)return <ErrorBox message="Les paramètres sont déjà configurés."/>; if(!configure&&!session.parametres)return <ErrorBox message="Les paramètres ne sont pas encore configurés."/>
 return <><PageHeader title={configure?"Configurer les paramètres":"Modifier les paramètres"} description="Choisissez les modules, seuils, directives, consignes et documents exigés." backTo={`/app/sessions/${sessionId}`}/>{error&&<div className="mb-5"><ErrorBox message={error}/></div>}<form onSubmit={submit} className="space-y-6"><Card><CardContent className="p-6 sm:p-8"><div className="space-y-2"><Label>Mode d’entrée *</Label><select className={selectClass} value={form.mode_entree} onChange={(e)=>setField("mode_entree",e.target.value as SessionParametersPayload["mode_entree"])}>{Object.entries(MODE_LABELS).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{([['hebergement_active','Hébergement'],['repas_active','Repas'],['visite_medicale_active','Visite médicale'],['activites_active','Activités'],['evaluation_active','Évaluations'],['attestation_active','Attestations'],['consultation_publique_active','Consultation publique']] as const).map(([k,l])=><label key={k} className="flex items-center justify-between rounded-xl border p-4"><span>{l}</span><input type="checkbox" className="size-5 accent-primary" checked={form[k]} onChange={(e)=>setField(k,e.target.checked)}/></label>)}</div></CardContent></Card>{form.attestation_active&&<Card><CardContent className="p-6 sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><div className="space-y-2"><Label>Présence minimale (%)</Label><Input type="number" min={0} max={100} value={form.taux_presence_minimum_attestation} onChange={(e)=>setField("taux_presence_minimum_attestation",Number(e.target.value))}/></div><div className="space-y-2"><Label>Moyenne minimale (/20)</Label><Input type="number" min={0} max={20} step="0.5" value={form.moyenne_minimum_attestation} onChange={(e)=>setField("moyenne_minimum_attestation",Number(e.target.value))}/></div></div></CardContent></Card>}<Card><CardContent className="p-6 sm:p-8"><div className="grid gap-5"><div className="space-y-2"><Label>Directives générales</Label><p className="text-sm text-muted-foreground">Orientations officielles de la session.</p><textarea rows={5} className={textAreaClass} value={form.directives_generales} onChange={(e)=>setField("directives_generales",e.target.value)}/></div><div className="space-y-2"><Label>Consignes générales</Label><p className="text-sm text-muted-foreground">Règles pratiques à respecter.</p><textarea rows={5} className={textAreaClass} value={form.consignes_generales} onChange={(e)=>setField("consignes_generales",e.target.value)}/></div><div className="space-y-3"><Label>Documents exigés</Label><p className="text-sm text-muted-foreground">Uniquement les pièces à fournir, par exemple une copie de la CNIB.</p><div className="flex gap-3"><Input value={document} onChange={(e)=>setDocument(e.target.value)} placeholder="Ex. Copie de la CNIB"/><Button type="button" variant="outline" onClick={()=>{const v=document.trim();if(v){setField("documents_exiges",[...form.documents_exiges,v]);setDocument("")}}}><Plus className="mr-2 size-4"/>Ajouter</Button></div>{form.documents_exiges.map((v,i)=><div key={`${v}-${i}`} className="flex items-center justify-between rounded-xl border px-4 py-3"><span>{v}</span><Button type="button" variant="ghost" onClick={()=>setField("documents_exiges",form.documents_exiges.filter((_,x)=>x!==i))}><Trash2 className="size-4"/></Button></div>)}</div></div></CardContent></Card><div className="flex justify-end gap-3"><Button type="button" variant="outline" onClick={()=>navigate(`/app/sessions/${sessionId}`)}>Annuler</Button><Button type="submit" disabled={submitting}>{submitting?"Enregistrement…":"Enregistrer"}</Button></div></form></>
}
